<%= render 'pages/home' %>

<h1>Most Recent Posts</h1>

<table id="post-info">
  <tr>
    <th>Article Title</th>
    <th>URL</th>
    <th>Blabber</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @post.each do |post| %>
  <tr>
    <td><%= link_to post.title, post %></td>
    <td><%= auto_link(post.URL) %></td>
    <td><%= post.blabber %></td>
   
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
